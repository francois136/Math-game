{
  "name": "@fw/client",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Web client: canvas rendering, function input with live preview, lobby and match UI.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@fw/contracts": "workspace:*",
    "@fw/core-math": "workspace:*",
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  }
}
