{
  "name": "@fw/physics",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Adaptive curve tracing, collisions, map generation and validation. Pure, no I/O.",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc --build",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@fw/contracts": "workspace:*"
  }
}
