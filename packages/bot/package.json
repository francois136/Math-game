{
  "name": "@fw/bot",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Picks a shot for a seat nobody is sitting in. Pure, no I/O.",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc --build",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@fw/contracts": "workspace:*",
    "@fw/rules": "workspace:*"
  },
  "devDependencies": {
    "@fw/core-math": "workspace:*",
    "@fw/physics": "workspace:*"
  }
}
