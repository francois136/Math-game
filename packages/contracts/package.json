{
  "name": "@fw/contracts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Frozen public contracts shared by every FunctionWars package: types, wire schemas, ports, error codes, defaults.",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc --build",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "zod": "^4.1.12"
  }
}
